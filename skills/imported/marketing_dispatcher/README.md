Skills folder: contains small skill stubs and examples.

Next steps:
- Replace stubs with production logic and secure connector credentials via repo secrets.
- Add tests for each skill under tests/ for CI.
- Add metadata for each skill (author, version) if required by platform.
