# 3D Render Studio References

Imported from external Apex skill assets.

## Source Assets

- `renderapp.html`: HTML artifact related to render app workflows and prompt-driven 3D visualization.

## Runtime Notes

- The `SKILL.md` wrapper is the runtime-readable skill surface.
- The HTML source is preserved as reference material, not executed automatically.
- The imported HTML was sanitized: the original hardcoded Gemini key was replaced with `REDACTED_CONFIGURE_SERVER_SIDE`.
